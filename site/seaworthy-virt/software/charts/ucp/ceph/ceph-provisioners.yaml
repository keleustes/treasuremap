---
apiVersion: armada.airshipit.org/v1alpha1
kind: ArmadaChart
metadata:
  name: ucp-ceph-provisioners
  replacement: true
  layeringDefinition:
    abstract: false
    layer: site
    parentSelector:
      name: ucp-ceph-provisioners
    actions:
      - method: merge
        path: .
spec:
  values:
    deployment:
      cephfs_provisioner: false
...
