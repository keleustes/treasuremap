---
apiVersion: armada.airshipit.org/v1alpha1
kind: ArmadaChart
metadata:
  layeringDefinition:
    abstract: false
    actions:
    - method: merge
      path: .
    layer: site
    parentSelector:
      name: ucp-ceph-provisioners
  name: ucp-ceph-provisioners
  replacement: true
spec:
  values:
    deployment:
      cephfs_provisioner: false
