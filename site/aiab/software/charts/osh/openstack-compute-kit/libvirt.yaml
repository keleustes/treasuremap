---
apiVersion: armada.airshipit.org/v1alpha1
kind: ArmadaChart
metadata:
  name: libvirt
  replacement: true
  layeringDefinition:
    abstract: false
    layer: site
    parentSelector:
      name: libvirt-global
      component: libvirt
    actions:
      - method: merge
        path: .values
      - method: delete
        path: .values.ceph_client
spec:
  values:
    conf:
      ceph:
        enabled: false
...
