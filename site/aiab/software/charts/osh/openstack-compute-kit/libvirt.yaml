---
apiVersion: armada.airshipit.org/v1alpha1
kind: ArmadaChart
metadata:
  layeringDefinition:
    abstract: false
    actions:
    - method: merge
      path: .values
    - method: delete
      path: .values.ceph_client
    layer: site
    parentSelector:
      component: libvirt
      name: libvirt-global
  name: libvirt
  replacement: true
spec:
  values:
    conf:
      ceph:
        enabled: false
