---
apiVersion: armada.airshipit.org/v1alpha1
kind: ArmadaChart
metadata:
  labels:
    name: ucp-mariadb-type
  layeringDefinition:
    abstract: false
    actions:
    - method: merge
      path: .
    layer: type
    parentSelector:
      name: ucp-mariadb-global
  name: ucp-mariadb
  replacement: true
spec:
  values:
    pod:
      replicas:
        ingress: 1
        server: 1
