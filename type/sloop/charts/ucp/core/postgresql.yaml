---
apiVersion: armada.airshipit.org/v1alpha1
kind: ArmadaChart
metadata:
  labels:
    name: ucp-postgresql-type
  layeringDefinition:
    abstract: false
    actions:
    - method: merge
      path: .
    layer: type
    parentSelector:
      name: ucp-postgresql-global
  name: ucp-postgresql
  replacement: true
spec:
  values:
    pod:
      replicas:
        server: 1
