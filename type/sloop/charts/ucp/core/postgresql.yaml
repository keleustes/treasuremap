---
apiVersion: armada.airshipit.org/v1alpha1
kind: ArmadaChart
metadata:
  name: ucp-postgresql
  replacement: true
  labels:
    name: ucp-postgresql-type
  layeringDefinition:
    abstract: false
    layer: type
    parentSelector:
      name: ucp-postgresql-global
    actions:
      - method: merge
        path: .
spec:
  values:
    pod:
      replicas:
        server: 1
...
