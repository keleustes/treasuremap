---
apiVersion: armada.airshipit.org/v1alpha1
kind: ArmadaChart
metadata:
  labels:
    name: horizon-type
  layeringDefinition:
    abstract: false
    actions:
    - method: merge
      path: .
    layer: type
    parentSelector:
      name: horizon-global
  name: horizon
  replacement: true
spec:
  values:
    pod:
      replicas:
        server: 1
