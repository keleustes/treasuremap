---
apiVersion: armada.airshipit.org/v1alpha1
kind: ArmadaChart
metadata:
  name: fluentd
  labels:
    name: fluentd-type
  layeringDefinition:
    abstract: false
    layer: type
    parentSelector:
      hosttype: fluentd-global
    actions:
      - method: merge
        path: .
spec:
  values:
    pod:
      replicas:
        fluentd: 1
...
