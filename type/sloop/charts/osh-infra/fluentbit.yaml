---
apiVersion: armada.airshipit.org/v1alpha1
kind: ArmadaChart
metadata:
  name: fluentbit
  labels:
    name: fluentbit-type
  layeringDefinition:
    abstract: false
    layer: type
    parentSelector:
      hosttype: fluentbit-global
    actions:
      - method: merge
        path: .
spec:
  values:
    pod:
      replicas:
        fluentd: 1
...
